///
/// HybridVariableBlurComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#import "HybridVariableBlurComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridVariableBlurSpecSwift.hpp"
#import "VariableBlur-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::variableblur;
using namespace margelo::nitro::variableblur::views;

/**
 * Represents the React Native View holder for the Nitro "VariableBlur" HybridView.
 */
@interface HybridVariableBlurComponent: RCTViewComponentView
@end

@implementation HybridVariableBlurComponent {
  std::shared_ptr<HybridVariableBlurSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridVariableBlurComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridVariableBlurComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridVariableBlurSpec> hybridView = VariableBlur::VariableBlurAutolinking::createVariableBlur();
    _hybridView = std::dynamic_pointer_cast<HybridVariableBlurSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  VariableBlur::HybridVariableBlurSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridVariableBlurProps const>(props);
  auto& newViewProps = const_cast<HybridVariableBlurProps&>(newViewPropsConst);
  VariableBlur::HybridVariableBlurSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // blurRadius: optional
  if (newViewProps.blurRadius.isDirty) {
    swiftPart.setBlurRadius(newViewProps.blurRadius.value);
    newViewProps.blurRadius.isDirty = false;
  }
  // direction: optional
  if (newViewProps.direction.isDirty) {
    swiftPart.setDirection(newViewProps.direction.value);
    newViewProps.direction.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

@end
