///
/// HybridVariableBlurComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridVariableBlurComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::variableblur::views {

  extern const char HybridVariableBlurComponentName[] = "VariableBlur";

  HybridVariableBlurProps::HybridVariableBlurProps(const react::PropsParserContext& context,
                                                   const HybridVariableBlurProps& sourceProps,
                                                   const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    blurRadius([&]() -> CachedProp<std::optional<double>> {
      try {
        const react::RawValue* rawValue = rawProps.at("blurRadius", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.blurRadius;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<double>>::fromRawValue(*runtime, value, sourceProps.blurRadius);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VariableBlur.blurRadius: ") + exc.what());
      }
    }()),
    direction([&]() -> CachedProp<std::optional<Direction>> {
      try {
        const react::RawValue* rawValue = rawProps.at("direction", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.direction;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<Direction>>::fromRawValue(*runtime, value, sourceProps.direction);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VariableBlur.direction: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridVariableBlurSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridVariableBlurSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("VariableBlur.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridVariableBlurProps::HybridVariableBlurProps(const HybridVariableBlurProps& other):
    react::ViewProps(),
    blurRadius(other.blurRadius),
    direction(other.direction),
    hybridRef(other.hybridRef) { }

  bool HybridVariableBlurProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("blurRadius"): return true;
      case hashString("direction"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridVariableBlurComponentDescriptor::HybridVariableBlurComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridVariableBlurComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                        const std::shared_ptr<const react::Props>& props,
                                                                                        react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridVariableBlurShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridVariableBlurComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridVariableBlurShadowNode&>(shadowNode);
    const HybridVariableBlurProps& props = concreteShadowNode.getConcreteProps();
    HybridVariableBlurState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::variableblur::views
